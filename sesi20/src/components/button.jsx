// Stateless component
import React from "react";


// export default (props) => {
//     return (
//         <button> {props.name} </button>
//     )
// }


const Button = (props) => {
    return (
        <button> {props.name} </button>
    )
}

export default Button